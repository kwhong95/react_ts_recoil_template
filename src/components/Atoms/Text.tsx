import { CSSProperties } from "react";
import styled from "styled-components";

const Text = styled.p<CSSProperties>`
  font-weight: bold;
  text-align: center;
  lign-height: 1.2;
`;

export default Text;
